Tik Tok Trend going to a profesional to tell you what your color pallet is based on skin tone eyecolor.
Why not let chat GPT do that for you? 

make sure you have python and node.js installed.
https://nodejs.org/en
https://www.python.org/downloads/

1. Clone the repository:

```bash
git clone git@github.com:TheBoiLeroy/ColorPallet.git

2.Create a venv environment (you can name the venv anything I like to call mines venv)
  
```bash
python -m venv venv

3.run your envirement (remember what you named your envirement if you didnt use venv)
windows
```bash
venv/Scripts/activate
mac
```bash
source venv/bin/activate

4.move into the backend and install the all dependencies in requirements.txt
```bash
cd backend
pip install -r requirements

5.


